# GitHub Upload 2026-05-02

This sync packages the current turn's newly accumulated project data into the
existing GitHub long-term project space for `twse2_prl_20260501` on branch
`open-ai` of `shangx108-code/opanai`.

Uploaded scope for this turn:

- input notes copied from the user upload queue into `inputs/`
- the `results/alpha_btk_correlation_2026-05-02/` result package

Non-goal for this turn:

- overwrite newer remote manuscript-facing files that already exist in the
  GitHub project space but are not present in this local snapshot
