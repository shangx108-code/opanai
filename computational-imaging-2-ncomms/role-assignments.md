# 角色任务表：计算成像2

## 统筹者
- 当前任务：维护项目阶段判断、唯一主瓶颈、唯一最高优先级，并完成当前工作区证据核查后的统一纠偏
- 输入：Nature Communications 门槛、当前命名空间状态文件、本轮真实计算输出
- 输出：每轮状态更新、优先级调整、已完成/未完成重新判定
- 最近完成产出：已确认 round1 / round2 / round3 在当前工作区均无实物文件，相关完成项已统一降级
- 下一步交付物：重建 round3 最小平台的执行入口和恢复顺序
- 完成标准：任何时刻都能明确当前阶段、当前主瓶颈、本轮最高优先级、交付物和下一轮动作
- 依赖：全部角色的真实完成状态
- 优先级：最高

## 理论人员
- 当前任务：暂停正式 `RCR` 推导定稿，先把推导前置条件重新定义为“round3 真实结果已重新落盘”
- 输入：当前状态文件中的理论目标与证据缺口
- 输出：
  - `RCR` 推导所需前提条件清单
  - 推导暂停原因与恢复条件
  - 定义 / 推导 / 假设三类边界的重置说明
- 最近完成产出：已明确没有真实结果文件时不得继续把操作性定义包装成已完成推导
- 下一步交付物：待 round3 真实结果恢复后再写可检查推导
- 完成标准：只有在真实结果与符号定义都回到当前工作区后，才恢复正式推导
- 依赖：代码与数值计算人员重建 round3 真实结果
- 优先级：P1

## 代码与数值计算人员
- 当前任务：重建并真实跑通最小线性高斯 round3 robust 平台
- 输入：当前状态文件中的指标目标、下一轮恢复要求
- 输出：
  - 重建后的 round3 脚本
  - 重新生成的 CSV / summary / log / 图文件
  - 可继续进入参数扫描的最小运行基座
- 最近完成产出：本轮确认当前工作区不存在任何可复核的 round3 实物文件
- 下一步交付物：重建后的 round3 最小结果包
- 完成标准：必须重新在当前工作区留下真实运行结果，而不是沿用记忆中的旧路径
- 依赖：统筹者给出的优先级与文件落盘要求
- 优先级：P1

## 数据分析人员
- 当前任务：暂停 round3 结果判读，先维护“缺失结果时不做结论判读”的边界
- 输入：当前证据核查结论
- 输出：
  - 当前禁止下结论的原因说明
  - round3 重建后需要立即检查的判读项目表
  - 对照分析的预登记清单
- 最近完成产出：已把此前基于缺失 round3 文件的判读全部降级为待重建
- 下一步交付物：等待新的 round3 结果表后再恢复判读
- 完成标准：没有真实结果文件就不输出排序结论
- 依赖：代码与数值计算人员重建 round3 结果
- 优先级：P2

## 画图人员
- 当前任务：暂停 Figure 5 正式字段推进，先把数据文件依赖重新列清
- 输入：当前证据核查结论、Figure 5 目标结构
- 输出：
  - Figure 5 所需真实数据文件清单
  - 重建后应立即生成的绘图字段表
  - 当前不可定稿原因说明
- 最近完成产出：已确认当前没有任何可作为 Figure 5 底稿的实物图文件
- 下一步交付物：等待 round3 结果恢复后再建立字段映射
- 完成标准：图表推进必须建立在当前工作区真实数据文件之上
- 依赖：代码与数值计算人员重建 round3 结果
- 优先级：P2

## 论文撰写人员
- 当前任务：暂停 round3 正式写作，把写作入口重置为“先恢复真实结果文件”
- 输入：当前证据核查结论、Nature Communications 写作门槛
- 输出：
  - 写作冻结说明
  - round3 恢复后才能写入 Results / Methods 的证据清单
  - 引言与结果段落的最小证据门槛表
- 最近完成产出：已撤回“已有真实 round3 数据可写作”的前提
- 下一步交付物：等待 round3 结果重建后再恢复 Results / Methods 骨架
- 完成标准：不对缺失数据写作，不把路线图写成结果
- 依赖：理论与代码结果恢复
- 优先级：P3

## 监督人员
- 当前任务：执行本轮证据完整性纠偏并锁定“无实物文件不算完成”的规则
- 输入：全部角色产出
- 输出：
  - 阶段性质检意见
  - 对“找不到实际文件”的旧条目做降级处理
  - 下一轮风险提醒
- 最近完成产出：已确认 round1 / round2 / round3 在当前工作区均未找到，对应完成项已统一降级
- 下一步交付物：监督轮次 4 的正式记录
- 完成标准：所有不满足 First Principle 的内容都不计入完成项
- 依赖：全部角色
- 优先级：最高

## 严格审稿人
- 当前任务：继续维持预审入口条件，不启动正式五审稿人循环
- 输入：阶段性结果、图表、推导、文稿
- 输出：是否具备进入严格审稿循环的判定
- 最近完成产出：新增“当前工作区必须存在实物结果文件”这一预审硬门槛
- 下一步交付物：下一次阶段性接收概率更新
- 完成标准：只有当正文、图表、推导、文献链和可复核性都形成后，才进入正式五审稿人评估
- 依赖：理论、结果、图表、写作均形成可审材料
- 优先级：后置

## 当前依赖主链
1. 代码与数值计算人员重建 round3 最小平台并留下当前工作区实物文件
2. 理论人员据此恢复 `RCR` 推导
3. 数据分析人员恢复排序与风险判读
4. 画图人员建立 Figure 5 字段映射
5. 撰写人员恢复 Results / Methods 骨架
6. 监督人员再次核查“实物文件与状态记录一致性”

## 本轮唯一最高优先级任务拆解
- 任务名称：证据完整性审计与状态纠偏
- 负责人：统筹者 + 监督人员
- 实际完成情况：已完成
- 已输出：
  - 对当前工作区的 round1 / round2 / round3 证据核查结论
  - 所有缺失实物文件条目的降级记录
  - 下一轮“先重建真实结果链”的唯一优先级重置
- 完成标准核对：
  - 已完成最小范围路径核查
  - 已确认当前工作区不存在相应实物文件
  - 已停止把缺失文件对应的结果继续记为已完成
