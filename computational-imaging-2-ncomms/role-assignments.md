# 角色任务表：计算成像2

## 统筹者
- 当前任务：维护项目阶段判断、唯一主瓶颈、唯一最高优先级，并将下一轮唯一优先级收敛到 round3 robust 真实重建
- 输入：Nature Communications 门槛、当前命名空间状态文件、本轮真实计算输出
- 输出：每轮状态更新、优先级调整、已完成/未完成重新判定
- 最近完成产出：已完成 round2 最小 task-weighted 链的真实重建，并据此更新当前阶段与下一轮主任务
- 下一步交付物：round3 robust 重建入口与恢复顺序
- 完成标准：任何时刻都能明确当前阶段、当前主瓶颈、本轮最高优先级、交付物和下一轮动作
- 依赖：全部角色的真实完成状态
- 优先级：最高

## 理论人员
- 当前任务：以当前 round2 真实结果为锚点，起草 `TIG` 与 task risk 的详细推导结构，并为后续 `RCR` 预留接口
- 输入：当前状态文件中的理论目标、round2 真实结果文件、证据缺口
- 输出：
  - `TIG` / task risk 推导所需符号表和假设表
  - 从 linear-Gaussian posterior 到 ideal task MI 的连续推导草案
  - 定义 / 推导 / 假设三类边界说明
- 最近完成产出：已确认 round2 真实结果文件现已回到当前工作区，可恢复正式推导准备
- 下一步交付物：`TIG` 第一版可检查推导提纲
- 完成标准：推导必须逐步可检查，并明确哪些部分仍只是操作性定义
- 依赖：当前 round2 真实结果 + 下一轮 round3 结果恢复
- 优先级：P1

## 代码与数值计算人员
- 当前任务：重建并真实跑通 round3 robust 平台，同时保留本轮已恢复的 round2 结果链
- 输入：当前状态文件中的指标目标、已恢复的 round2 最小平台、下一轮恢复要求
- 输出：
  - 重建后的 round3 脚本
  - round3 的 CSV / summary / log / 图文件
  - 与 round2 可对照的最小 robust-capacity 结果包
- 最近完成产出：已真实生成 round2 脚本、CSV、summary、SVG 和运行日志
- 下一步交付物：round3 最小结果包
- 完成标准：必须在当前工作区留下 round3 实物文件，并能与 round2 共存对照
- 依赖：统筹者给出的优先级与文件落盘要求
- 优先级：P1

## 数据分析人员
- 当前任务：基于已恢复的 round2 结果做结构化判读，并为 round3 对照预登记分析项
- 输入：当前 round2 CSV / summary / SVG
- 输出：
  - `gamma = 0.0` 规范 task-matched 分离点的结构化结论
  - `gamma = 0.8` 左右 task-optimal 转移的最小解释
  - round3 需要直接对照的指标表
- 最近完成产出：已确认 reconstruction-optimal 与 task-optimal 的真实排序分离，并识别 task-optimal 转换点
- 下一步交付物：round3 对照判读模板
- 完成标准：只对当前真实结果做边界清晰的判读，不外推成普适结论
- 依赖：代码与数值计算人员重建 round3 结果
- 优先级：P2

## 画图人员
- 当前任务：基于当前 round2 真实结果维护 Figure 5 依赖清单，并等待 round3 结果加入对照
- 输入：当前 round2 结果文件、Figure 5 目标结构
- 输出：
  - Figure 5 所需真实数据文件清单
  - round3 恢复后应立即生成的绘图字段表
  - 当前不可定稿原因说明
- 最近完成产出：已拿到 round2 overview SVG，可作为后续 Figure 5 字段映射的最小底稿
- 下一步交付物：等待 round3 结果恢复后建立联合字段映射
- 完成标准：图表推进必须建立在当前工作区真实数据文件之上
- 依赖：代码与数值计算人员重建 round3 结果
- 优先级：P2

## 论文撰写人员
- 当前任务：继续暂停正式写作，但把 Results/Methods 的最小写作入口更新为“round2 + round3 真实文件齐备后恢复”
- 输入：当前 round2 真实结果、Nature Communications 写作门槛
- 输出：
  - 写作冻结说明
  - round2 / round3 齐备后才能写入 Results / Methods 的证据清单
  - 引言与结果段落的最小证据门槛表
- 最近完成产出：已确认当前可以合法引用 round2 最小结果，但仍不足以形成正文段落
- 下一步交付物：等待 round3 结果恢复后再恢复 Results / Methods 骨架
- 完成标准：不对缺失数据写作，不把路线图写成结果
- 依赖：理论与代码结果恢复
- 优先级：P3

## 监督人员
- 当前任务：监督 round2 真实结果链与状态记录一致，并锁定下一轮只攻 round3 重建
- 输入：全部角色产出
- 输出：
  - 阶段性质检意见
  - 对当前真实输出与状态记录的一致性核查
  - 下一轮风险提醒
- 最近完成产出：已完成对 round2 新生成文件的核查，并确认本轮状态更新基于真实结果
- 下一步交付物：监督轮次 6 的正式记录
- 完成标准：所有不满足 First Principle 的内容都不计入完成项
- 依赖：全部角色
- 优先级：最高

## 严格审稿人
- 当前任务：继续维持预审入口条件，不启动正式五审稿人循环
- 输入：阶段性结果、图表、推导、文稿
- 输出：是否具备进入严格审稿循环的判定
- 最近完成产出：新增“至少 round2 / round3 需同时具备当前工作区实物文件”这一预审硬门槛
- 下一步交付物：下一次阶段性接收概率更新
- 完成标准：只有当正文、图表、推导、文献链和可复核性都形成后，才进入正式五审稿人评估
- 依赖：理论、结果、图表、写作均形成可审材料
- 优先级：后置

## 当前依赖主链
1. 代码与数值计算人员重建 round3 robust 平台并留下当前工作区实物文件
2. 理论人员据 round2 / round3 结果恢复 `TIG` / `RCR` 与相关指标推导
3. 数据分析人员建立 recon / task / robust 三线对照判读
4. 画图人员建立 Figure 5 字段映射
5. 撰写人员恢复 Results / Methods 骨架
6. 监督人员再次核查“实物文件与状态记录一致性”

## 本轮唯一最高优先级任务拆解
- 任务名称：重建最小 round2 task-weighted 真实结果链
- 负责人：代码与数值计算人员 + 统筹者 + 监督人员
- 实际完成情况：已完成
- 已输出：
  - 当前工作区中的 round2 脚本
  - round2 CSV / summary / SVG / log
  - 基于真实结果的下一轮唯一优先级重置
- 完成标准核对：
  - 已完成真实运行
  - 已留下当前工作区实物文件
  - 已确认状态更新不再依赖缺失文件
