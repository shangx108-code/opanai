# 项目状态：计算成像2

## 项目基本信息
- 项目名称：计算成像2
- 目标期刊：Nature Communications
- 最近更新时间：2026-04-26
- 当前资料来源：当前项目命名空间状态文件 + 本轮对 `/workspace` 的最小范围证据核查结果

## 当前推进规则
- 之后默认使用“自研智能体”持续推进本项目。
- 自动迭代频率调整为每 2 小时 1 轮。
- 停止条件不只是接收概率超过 70%，还要求所有应补证据链全部补齐。
- 所有数据图必须基于真实数据生成，不允许用示意数据替代。
- 仅机制示意图、结构示意图和概念图可使用 GPT-imag-2.0 生成初稿，且仍需后续人工校正后才可进入正文或补充材料。
- 理论推导必须详实、连续、可检查；未完成详细推导的结论不得计为已完成证据。

## 研究总目标
围绕“计算成像中光学编码的信息容量、任务容量与鲁棒容量理论”建立一条可投稿到 Nature Communications 的完整证据链，最终形成：
- 主体理论：统一 forward model、Shannon 信息容量、Fisher 信息、任务容量、鲁棒容量及其相互关系
- 真实可复现实验链：至少覆盖线性压缩成像、PSF/depth 参数估计、任务导向快照成像、鲁棒误差分析 4 条主验证线
- 正文与补充材料全套投稿级图表
- 30 篇以上已核对参考文献
- 可归档的代码、数据、图表、正文、补充材料与最终投稿包

## 当前阶段
结果生成阶段（证据恢复子阶段）。

说明：
- 本轮对 `/workspace` 做了最小范围证据核查，未找到此前状态文件中记录的 round3 robust 脚本、结果表、总结文件、运行日志或 SVG 图。
- 当前工作区中同样未找到此前记忆中记录的 round1 / round2 脚本与结果文件。
- 因此，现阶段不能再把 round1 / round2 / round3 的任何数值结果继续计为“已真实完成”；项目已回退到“需要先恢复第一条可追溯真实证据链”的状态。

## 当前唯一主瓶颈
当前工作区内不存在任何可追溯的 round1 / round2 / round3 实物文件，导致项目缺少可复核的真实结果基座。

具体表现：
- 先前状态文件中列出的 round3 路径在当前工作区全部核查失败。
- 没有真实脚本与输出文件，就不能继续把 `RCR`、排序分离或 Figure 5 数据接口当作已建立证据。
- 在真实结果缺失的情况下，详细推导、正式图表和审稿概率都会失去落脚点。
- 参考文献仍明显不足，且当前也没有正文、补充材料或图表源文件可支撑投稿链条。

## 本轮唯一最高优先级
执行证据完整性审计并纠正状态文件，把所有缺少当前工作区实物支撑的“已完成”条目降级。

本轮只聚焦一件事：
- 核查 round1 / round2 / round3 的工作区实物是否存在
- 将不存在实物文件的结果从“已真实完成”中移除
- 重新定义下一轮唯一最高优先级为“重建并真实跑通第一条最小证据链”

## 本轮交付物
1. 本轮工作区证据核查结论
2. 纠偏后的阶段判定、唯一主瓶颈与唯一最高优先级
3. 对 round1 / round2 / round3 “已完成”状态的统一降级记录
4. 下一轮重建真实结果链的明确入口条件

## 完成标准
本轮仅在以下内容全部满足时视为完成：
- 已完成对当前工作区的 round1 / round2 / round3 证据核查
- 已把所有无实物文件支撑的结果从“已真实完成”中移除
- 已把下一轮任务收敛到“重建第一条真实结果链”，不再基于缺失文件继续推导或写作
- 已将本轮纠偏结果写入项目状态文件

## 项目停止条件
只有在以下条件同时满足后，项目才允许停止自动推进：
1. Nature Communications 五位审稿人的接收概率判断都大于 70%
2. 所有需要补的 evidence 均已补齐
3. 所有正文图和补充材料图均已补齐，其中数据图全部来自真实数据
4. 所有关键理论推导均已补齐并通过可检查性要求
5. 参考文献已补齐到 30 篇以上并完成核对

## 下一轮立即动作
1. 在当前工作区重建并真实跑通最小线性高斯 round3 robust 脚本，重新生成脚本、CSV、summary、log 和图文件
2. 只有在 round3 真实结果重新落盘后，才恢复 `RCR` 推导固化
3. 在重建完成后，再决定是继续补 round1 / round2 还是先做 Figure 5 数据映射
4. 同步恢复参考文献核对链，先建立 10 篇可核对种子文献
5. 继续维持“无实物文件不算完成”的监督规则

## 已真实完成
- 已识别目标期刊为 Nature Communications。
- 已完成项目初始化编排与持续迭代框架设计。
- 已在本轮完成当前工作区证据完整性核查，并确认 round1 / round2 / round3 的实物文件当前均不存在于 `/workspace`。
- 已把缺少当前工作区实物支撑的旧结果从“已真实完成”状态中移除。

## 已部分完成但仍缺关键环节
- 文献：30+ 已核对参考文献仍未建立。
- 理论：`RCR` 的理论路线仍只停留在命名与任务描述层，当前没有可与真实结果逐项对齐的详细推导稿。
- 可复核性：round1 / round2 / round3 全部缺少当前工作区实物文件，必须先重建真实证据链。
- 写作：虽然已有结果结构规划，但当前没有可合法引用的真实结果文件，因此正文和补充材料仍不能推进为正式写作。

## 尚未开始
- 当前工作区内可追溯的 round1 / round2 / round3 真实计算链
- 全套理论推导文稿
- PSF / depth 平台真实计算
- 正文和补充材料正式图生成
- 五位审稿人并行评审循环
- 最终投稿归档包

## 参考文献状态
- 当前已核对参考文献数：0
- 目标下限：30
- 当前判断：明显不足，尚不能支撑 Nature Communications 级别的引言、讨论和补充材料文献链

## 当前接收概率判断
- 综合接收概率：8%–14%

依据：
- 创新构想：强
- 理论充分性：弱到中
- 方法与代码可靠性：中
- 数据与结果完整性：弱到中
- 图表质量：弱到中
- 写作成熟度：弱
- 期刊匹配度：中到强

当前最拖累接收概率的短板：
1. 当前工作区没有任何可追溯的真实结果文件，证据基座为空
2. 理论推导、图表与写作都失去可复核输入，必须先恢复真实计算链
3. 参考文献、补充材料与跨平台验证远未补齐

## 最近一次重要更新摘要
2026-04-26：对当前工作区执行证据完整性核查后，发现此前状态文件记录的 round3 robust 路径同样不存在，且 round1 / round2 也未找到。因此本项目已回退到“结果生成阶段的证据恢复子阶段”，本轮最高优先级已改为“纠偏状态并为下一轮真实重建让路”，不再把任何缺失文件对应的结果继续计为已完成。
