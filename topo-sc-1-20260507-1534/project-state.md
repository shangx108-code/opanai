# Project State

- Current stage: `project-space initialization and theory-to-computation contract setup`
- Unique main bottleneck: `computational correctness and parameter-provenance gap for the first reusable Fe(Te,Se) baseline model`
- Why this is first: the paper line, material choice, and theory narrative are already locked from the intake markdown, but no verified executable baseline tied to the canonical project root has yet been returned by Codex.
- Latest real progress:
  - locked the canonical project identifier `topo-sc-1-20260507-1534`
  - created the canonical memory project root and stable subdirectory structure
  - copied the intake markdown into `user-materials/`
  - installed Codex-facing prompts and feedback templates into the canonical project root
  - prepared the ledger files needed for long-term orchestration
- Current accepted evidence:
  - the uploaded markdown supports a Nature Communications line centered on `strain-dislocation-vortex` co-control rather than a single strain-tuned vortex story
  - the primary materials line is `FeTe0.55Se0.45 / FeTe0.5Se0.5`
  - the theory side now has a stable execution brief and a reusable feedback interface for coding runs
- Immediate next action: `send WP1 and the one-shot execution prompt to Codex and wait for the first baseline-model feedback files`
- Archive status note: `canonical long-term space is initialized locally and being mirrored to GitHub under the fixed project root`
