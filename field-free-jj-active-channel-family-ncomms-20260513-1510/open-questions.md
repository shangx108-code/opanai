# Open Questions

- Should the Week-5 `w4_b0.3` family package remain a source-data-only support layer, or should it be promoted into a compact supplementary figure or table in the next revision cycle?
- Is `integrated_abs_G` the genuine limiting rung for this family, or only the dominant first failure mode inside the current narrow ladder?
- Is `L = 176` a genuine resonance mechanism or only a finite-size/end-environment sensitivity point?
- Are the current conclusions robust to:
  - rotated compensated textures
  - alternative harmonics
  - shifted / asymmetric / smooth-barrier active-channel geometries
  - random disorder seed ensembles
- Does the unique held-out all-pass transport row remain credible after reverse audit?
- When the fixed GitHub archive path is available, should the uploaded Week-5 zip be mirrored as a raw intake artifact, or should the canonical archive keep only the integrated source-data folder plus manifest references?
